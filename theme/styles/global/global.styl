/*! Default styles from your beloved manGoweb (http://mangoweb.cz) */

*, *:before, *:after
	box-sizing border-box

*, *:focus
	highlight none
	outline none

html
	font-size: $base-font-size

body
	color $text-color
	font-family $font-family
	font-size $base-font-size
	line-height $gratio
	overflow-x hidden
	margin 0

a
	color $c-main
	text-decoration underline

h1, h2, h3, h4, h5, h6
	line-height 1
	margin $d-spacing*2 0 1em
	text-rendering optimizeLegibility

img
	max-width 100%
	height auto
	vertical-align middle

p
	margin 0 0 $d-spacing

	img
		margin-bottom $d-spacing

ol, ul, li
	margin 0
	padding 0

blockquote
	margin $d-spacing 0
	padding $d-padding 0 $d-padding $d-spacing
	border-left 2px solid
	color $text-color-light

table
	width 100%

input,
button,
select,
textarea
	font-family inherit
	font-size inherit
	line-height inherit

svg
	pointer-events none

b, i
	font-style normal
	font-weight normal


// Helper classes

.accessible
	position: absolute !important
	clip: rect(1px 1px 1px 1px) /* IE6, IE7 */
	clip: rect(1px, 1px, 1px, 1px)
	overflow hidden
	size 1px 1px
.fleft
	float left
.fright
	float right
.clear
	clear both

.thumb-circle
	border-radius 50%
	overflow hidden

